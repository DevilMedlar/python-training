# Continue the same Python lesson

Use [the single launch prompt](START_PROMPT.md). This filename remains so old
links work. GitHub is learned through the current Python exercise: its commit,
output, tests, branch, or pull request. Begin at `P1-01` if you are new to Python,
and use the `github_skills` references attached to that lesson.

All learner work uses github.com and github.dev, with execution in GitHub Actions.
There is no separate GitHub starting route or progress record.
